// Tutor public profile page — re-exports the user profile component.
// Rendered within the tutor layout so the sidebar/header stay correct.
// useDashBase() inside the component will detect /tutor prefix automatically.
export { default } from "@/app/(dashboard)/user/profile/[uid]/page"
