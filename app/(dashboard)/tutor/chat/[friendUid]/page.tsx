// Tutor chat page — identical functionality to the user chat page.
// Rendered within the tutor layout so the sidebar/header stay correct.
export { default } from "@/app/(dashboard)/user/chat/[friendUid]/page"
