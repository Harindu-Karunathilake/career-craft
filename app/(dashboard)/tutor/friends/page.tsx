// Tutor friends page — identical functionality to the user friends page.
// Rendered within the tutor layout so the sidebar/header stay correct.
export { default } from "@/app/(dashboard)/user/friends/page"
